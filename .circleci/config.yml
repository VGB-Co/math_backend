version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: Print the Current Time
          command: date
      # - run:
        # name: Update code on aws
        # command: ssh -i "rft-math.pem" ec2-user@ec2-3-16-89-127.us-east-2.compute.amazonaws.com
